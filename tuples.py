thistuple = ("Jan","Feb","Mar")
print(thistuple) # Prints the complete tuple
print(thistuple[0]) # Prints first element of the tuple print months[1:3]
# Prints elements starting from the second to the third print months [2:]
# Prints elements that starting from the third element print months * 2 
# Prints tuple two times 
print(thistuple + thistuple)
# Prints the concatenated tuple